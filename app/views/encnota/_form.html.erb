<%= form_for(encnotum) do |f| %>
  <% if encnotum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(encnotum.errors.count, "error") %> prohibited this encnotum from being saved:</h2>

      <ul>
      <% encnotum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idgru %>
    <%= f.number_field :grupo_id %>
  </div>

  <div class="field">
    <%= f.label :idcu %>
    <%= f.number_field :curso_id %>
  </div>

  <div class="field">
    <%= f.label :idasi %>
    <%= f.number_field :asignatura_id %>
  </div>

  <div class="field">
    <%= f.label :iddoce %>
    <%= f.number_field :docente_id %>
  </div>

  <div class="field">
    <%= f.label :idpe %>
    <%= f.number_field :periodo_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
