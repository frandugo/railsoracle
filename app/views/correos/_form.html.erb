<%= form_for(correo) do |f| %>
  <% if correo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(correo.errors.count, "error") %> prohibited this correo from being saved:</h2>

      <ul>
      <% correo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :correo %>
    <%= f.email_field :correo %>
  </div>

  <div class="field">
    <%= f.label :idnom %>
    <%= f.number_field :estudiante_id %>
  </div>

  <div class="field">
    <%= f.label :iddoce %>
    <%= f.number_field :docente_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
