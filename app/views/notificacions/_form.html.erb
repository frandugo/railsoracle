<%= form_for(notificacion) do |f| %>
  <% if notificacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notificacion.errors.count, "error") %> prohibited this notificacion from being saved:</h2>

      <ul>
      <% notificacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :notificacion %>
    <%= f.text_area :notificacion %>
  </div>

  <div class="field">
    <%= f.label :iddoce %>
    <%= f.number_field :docente_id %>
  </div>

  <div class="field">
    <%= f.label :idnom %>
    <%= f.number_field :estudiante_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
