<p id="notice"><%= notice %></p>

<h1>Profesions</h1>

<table>
  <thead>
    <tr>
      <th>Tarjeta Profesional</th>
      <th>Titulo Profesional</th>
      <th>docente_id</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profesions.each do |profesion| %>
      <tr>
        <td><%= profesion.tarpro %></td>
        <td><%= profesion.nompro %></td>
        <td><%= profesion.docente_id %></td>
        <td><%= link_to 'Show', profesion %></td>
        <td><%= link_to 'Edit', edit_profesion_path(profesion) %></td>
        <td><%= link_to 'Destroy', profesion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Profesion', new_profesion_path %>
